import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#91B6E2',
      main: '#026AA7',
    },
    secondary: {
      light: '#ecf0f2',
      main: '#B3CFF2',
    },
  },
});
